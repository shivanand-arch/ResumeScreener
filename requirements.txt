streamlit==1.50.0
# Authlib 1.6.6 broke streamlit[auth] OAuth callback handling — every
# st.login("google") completion crashed with `400: 'NoneType' object does not
# support item assignment`. Streamlit Cloud rebuilds the env periodically and
# silently resolved to the broken Authlib once it hit PyPI (May 2026). Cap
# below 1.6.6 until we upgrade to Streamlit >=1.52.2 (which includes the fix).
# Refs: streamlit/streamlit#13335, streamlit/streamlit#13461
authlib>=1.3.2,<1.6.6
anthropic>=0.40.0
pdfplumber>=0.11.0
python-docx>=1.1.0
openpyxl>=3.1.0
pdf2image>=1.17.0
pytesseract>=0.3.10
requests>=2.31.0
